version: '3'
services:
  db:
    image: postgres
    volumes:
      - ./tmp/postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
  web:
    depends_on:
      - 'db'
    build:
      context: .
      dockerfile: Dockerfile
    image: study_diary/app
    command: /bin/bash -c "rm -f /tmp/server.pid && bundle exec rails server -b 0.0.0.0 -P /tmp/server.pid"
    ports:
      - 3000:3000
    volumes:
      - .:/app
    networks:
      - default
